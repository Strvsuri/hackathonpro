
kind: Deployment 
apiVersion: apps/v1
metadata:
  name: nodedep
spec:
  replicas: 1
  selector:
    matchExpressions:
      - {key: env, operator: In, values: [test]}
  template:
    metadata:
      name: nodepod
      labels:
        env: test
    spec:
      containers:
        - name: nodecont
          image: nginx
          ports:
            - containerPort: 80
